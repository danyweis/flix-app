<h1>Movies listing</h1>

<% @movies.each do |movie| %>
    <li><strong><%= movie.title %></strong> (<%= movie.rating %>): 
    <%= number_to_currency(movie.total_gross, precision: 0 ) %></li>
    <p><%= movie.description %></p>
    <p><%= movie.released_on %></p>
    
<% end %>